import Typography from "@mui/material/Typography";
import React from "react";

function Service() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        flexDirection: "column",
      }}
    >
      <Typography>
        I provinding service like web development, freelencing
      </Typography>
    </div>
  );
}

export default Service;
